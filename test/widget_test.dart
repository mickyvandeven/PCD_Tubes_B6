// Basic smoke test untuk FatScan app

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FatScan smoke test', (WidgetTester tester) async {
    // Test placeholder - implementasi penuh memerlukan Hive mock
    expect(1 + 1, equals(2));
  });
}
